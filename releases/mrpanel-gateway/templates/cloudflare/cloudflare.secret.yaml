apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.cloudflare.secretName }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  {{ .Values.cloudflare.certApiTokenKey | quote }}: {{ .Values.global.secrets.mrpanelGateway.cloudflare.apiKey | b64enc }}